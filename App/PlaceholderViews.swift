import SwiftUI

// Placeholder view for HomeView to be replaced later
struct HomeView: View {
    var body: some View {
        Text("HomeView Placeholder")
    }
}

// Placeholder view for SearchView to be replaced later
struct SearchView: View {
    var body: some View {
        Text("SearchView Placeholder")
    }
}

// Placeholder view for YourListView to be replaced later
struct YourListView: View {
    var body: some View {
        Text("YourListView Placeholder")
    }
}

// Placeholder view for LeaderboardView to be replaced later
struct LeaderboardView: View {
    var body: some View {
        Text("LeaderboardView Placeholder")
    }
}

// Placeholder view for ProfileView to be replaced later
struct ProfileView: View {
    var body: some View {
        Text("ProfileView Placeholder")
    }
}

// Placeholder view for AuthenticationView to be replaced later
struct AuthenticationView: View {
    var body: some View {
        Text("AuthenticationView Placeholder")
    }
}

struct HomeView_Previews: PreviewProvider {
    static var previews: some View {
        HomeView()
    }
}
