<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Core bundle metadata -->
    <key>CFBundleName</key>
    <string>MovieRanker</string>
    <key>CFBundleDisplayName</key>
    <string>MovieRanker</string>
    <key>CFBundleIdentifier</key>
    <string>com.example.MovieRanker</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>

    <!-- iOS specific -->
    <key>LSRequiresIPhoneOS</key>
    <true/>

    <!-- Deployment target keys are handled by build settings; not set here. -->

    <!-- Launch screen/storyboard keys are optional; uncomment and adjust if your project uses them. -->
    <!--
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>
    -->

    <!-- UIKit main storyboard (usually not needed for SwiftUI lifecycle) -->
    <!--
    <key>UIMainStoryboardFile</key>
    <string>Main</string>
    -->

    <!-- Add privacy usage descriptions only for APIs you actually use. Examples: -->
    <!--
    <key>NSCameraUsageDescription</key>
    <string>This app requires camera access to capture photos.</string>
    <key>NSPhotoLibraryUsageDescription</key>
    <string>This app requires photo library access to select images.</string>
    -->
</dict>
</plist>
